Ernest

## Build cache (sccache)

Install sccache and use the cached build script:

```bash
sudo apt install sccache
npm run build:tauri:sccache
```

If sccache is not available, continue using `npm run build:tauri`.
